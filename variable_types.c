#include <stdio.h>

int main() {
	
	// define variable
	char first_a = 'a';

	// this variable is declared with no definition
	// well, technically the definition is assigned a garbage value
	float b;

	// declare and define multiple on one line
	int _c, _d, e;

	// print variable
	printf("%c \n", first_a);

	return 0;
}
